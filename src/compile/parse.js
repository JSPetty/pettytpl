import {commonUtils} from '../utils/commonUtils'

class parse {
  constructor() {
    this.sections = [];
    this.tokens = [];
    this.spaces = [];
    this.hasTag = false;
    this.nonSpace = false;


  }

  parseTemplate(cfg) {
    // cfg.scannerInc.scan(cfg);
  }

}

export {parse}
